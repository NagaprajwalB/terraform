# example.auto.tfvars
# Any file ending in .auto.tfvars or .auto.tfvars.json is loaded
# automatically, alongside terraform.tfvars — useful for splitting
# variable values across multiple logical files (e.g. secrets.auto.tfvars,
# network.auto.tfvars) without needing -var-file flags.

instance_type = "t3.small"
